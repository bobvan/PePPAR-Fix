# Core: observation pipeline, PPP filter, PHC servo
numpy
pyubx2>=1.2.55
pyserial
pyrtcm>=1.1.0

# Lab-hardware hosts: I2C (Renesas 8A34002 TDC / AD5693R DAC) and GPIO
# (TADD divider ARM pin).  gpiod is a Linux-only C-extension — without it
# the divider ARM silently no-ops and the DO PPS never GNSS-locks.
smbus2
gpiod; sys_platform == "linux"

# Analysis: TDEV/ADEV plots, servo log analysis
allantools
matplotlib
pandas

# peppar-mon (status display) deps live in requirements-mon.txt so the
# engine's dependency surface stays tight — lab hosts that never run the
# monitor don't need Textual or its rich/markdown-it dep chain.
